# Docs

Label texts and background material. Parts lists belong in `03 hardware/`. A sub folder such as `00_findings/` appears on the website when it has its own `youdecide.json` and is listed in `sections` of the exhibit's `youdecide.json`.
